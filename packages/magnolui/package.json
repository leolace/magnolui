{
  "name": "@magnolui/react",
  "version": "1.0.0",
  "description": "",
  "main": "./src/index.tsx",
  "scripts": {
    "typecheck": "tsc -b --noEmit",
    "build:tsc": "tsc -b tsconfig.build.json",
    "build:css": "sass --no-source-map --style=compressed ./src:./dist",
    "build": "pnpm clear:dist && pnpm build:tsc && pnpm build:css && pnpm replace-scss-imports",
    "clear:dist": "rm -rf ./dist",
    "replace-scss-imports": "bash ./scripts/replace-scss-imports.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.12.1",
  "peerDependencies": {
    "react": ">=18.1",
    "react-dom": ">=18.1",
    "typescript": ">=5"
  },
  "devDependencies": {
    "sass-embedded": "^1.89.2"
  }
}
